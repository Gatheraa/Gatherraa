[package]
name = "gathera-common"
version = "0.1.0"
edition = "2021"
description = "Common utilities and types for Gatheraa platform contracts"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = "23.5.2"

[features]
testutils = ["soroban-sdk/testutils"]
